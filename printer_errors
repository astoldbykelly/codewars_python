# solution
def printer_error(s):
    color_letters = 'abcdefghijklm'
    num=0
    den=len(s)

    for letter in s:
        if letter not in color_letters:
            num+=1

    return str(num) + "/" + str(den)
# explanation
# num is numerator, den is denominator
# denominator is the total length of the string
# numerator only looks for letters not a-m
